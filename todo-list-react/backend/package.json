{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "migrate": "bunx prisma migrate dev",
    "generate": "bunx prisma generate"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "bun-types": "latest",
    "typescript": "^5.x"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.1.0",
    "@prisma/client": "^5.x",
    "prisma": "^5.x",
    "zod": "^3.x"
  }
}
